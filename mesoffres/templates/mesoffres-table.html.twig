<table class="table">
  <thead>
    <tr>
      <th scope="col">Date</th>
      <th scope="col">Entreprise</th>
      <th scope="col">Intitulé de l'offre</th>
      <th scope="col">Nom du contact</th>
      <th scope="col">Mail du contact</th>
      <th scope="col">Réponse</th>
    </tr>
  </thead>
  <tbody>
    {% if offres %}
      {% for offre in offres%}
        <tr>
          <th scope="row">{{ offre.date }}</th>
          <td>{{ offre.entreprise }}</td>
          <td>{{ offre.intitule }}</td>
          <td>{{ offre.nom_contact }}</td>
          <td>{{ offre.mail_contact }}</td>
          <td>{{ offre.reponse }}</td>
        </tr>
      {% endfor %}
    {% else %}
      <tr>
        <td>Aucune offre n'a été trouvée</td>
      </tr>
    {% endif %}
  </tbody>
</table>
<div class="paginator">
  <nav aria-label="Page navigation example">
    <ul class="pagination">
      <li class="page-item"><a class="page-link" href="#">Previous</a></li>
      <li class="page-item"><a class="page-link" href="#">1</a></li>
      <li class="page-item"><a class="page-link" href="#">2</a></li>
      <li class="page-item"><a class="page-link" href="#">3</a></li>
      <li class="page-item"><a class="page-link" href="#">Next</a></li>
    </ul>
  </nav>
</div>
<div class="ajout_offre">
  <a href="{{ path('mesoffres.ajout') }}" class="ajouter btn btn-success">Ajouter une offre</a>
</div>
